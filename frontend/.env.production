# Application
VITE_APP_NAME=YORA PET
VITE_APP_VERSION=0.1.0

# API — same-origin (nginx proxies /api and /uploads to uvicorn :8000).
# Avoids mixed content when the app is served over HTTPS.
VITE_API_BASE_URL=/api/v1

# Feature flags
VITE_ENABLE_API_HEALTH_CHECK=true
