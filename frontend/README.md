# See root README.md for setup with pnpm.

```bash
pnpm install
pnpm run dev      # http://localhost:5173
pnpm run test
pnpm run build
```

Unit tests live in `src/test/`. Playwright e2e scaffold: `e2e/README.md`.
