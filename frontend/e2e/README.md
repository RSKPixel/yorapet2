# End-to-end tests (Playwright)

Add Playwright specs here when browser automation is required.

Suggested bootstrap:

```bash
pnpm create playwright
```

Keep e2e tests separate from unit tests in `src/test/`.
